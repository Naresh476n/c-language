#include <stdio.h>

int main()
{
    int a = 5, b = 6, sum;

    sum = a + b;

    printf("The sum of a and b is %d", sum);          // Method 1: Using 'sum' variable
    printf("\n the sum of a+b=%d", a + b);            // Method 2: Direct expression

    return 0;
}
